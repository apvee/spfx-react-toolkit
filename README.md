# spfx-react-toolkit
React runtime &amp; hooks for SharePoint Framework (SPFx). A single SPFxProvider with strongly-typed, enterprise-grade hooks: theme, display mode, properties (read/write), placeholders, list/field slices, HTTP/AAD, site/permissions, Teams, environment, container size, visibility/focus, performance, and structured logging.
